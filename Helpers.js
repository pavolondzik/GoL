// dont need this now
//function concat(string1, string2)
//{
//    if (typeof string1 == "string" || string1 instanceof String)
//        if (typeof string2 == "string" || string2 instanceof String)
//            return string1.concat(string2);
//}

function random()
{
    return Math.random() < 0.5 ? true : false;
}